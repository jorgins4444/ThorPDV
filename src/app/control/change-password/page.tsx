import '../control.css';
import { controlChangePassword } from './actions';

export default async function ControlChangePasswordPage({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <main className="control-auth-shell"><section className="control-auth-card compact"><div className="control-brand"><span>ϟ</span><div><strong>THOR CONTROL</strong><small>Primeiro acesso</small></div></div><div className="control-auth-copy"><small>SEGURANÇA</small><h1>Defina sua senha definitiva</h1><p>A senha temporária será invalidada assim que esta alteração for concluída.</p></div><form action={controlChangePassword} className="control-auth-form"><label>Nova senha<input name="password" type="password" minLength={8} required autoFocus/></label><label>Confirmar senha<input name="confirm" type="password" minLength={8} required/></label>{q.error&&<div className="control-alert error">{q.error}</div>}<button type="submit">Salvar nova senha</button></form></section></main>}
