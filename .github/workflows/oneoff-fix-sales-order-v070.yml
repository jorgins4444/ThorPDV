name: One-off Fix Sales Order v0.7.0

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/oneoff-fix-sales-order-v070.yml'

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fix JSX search input
        shell: python
        run: |
          from pathlib import Path
          p=Path('src/app/dashboard/[...slug]/sales-order-workspace.tsx')
          s=p.read_text(encoding='utf-8')
          old='<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Número ou cliente"><button'
          new='<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Número ou cliente"/><button'
          if s.count(old)!=1: raise SystemExit('target not found')
          p.write_text(s.replace(old,new),encoding='utf-8')
      - name: Commit
        run: |
          git config user.name "ThorPDV Automation"
          git config user.email "actions@users.noreply.github.com"
          git add 'src/app/dashboard/[...slug]/sales-order-workspace.tsx'
          git commit -m "fix: close sales order search input correctly"
          git push origin HEAD:main
