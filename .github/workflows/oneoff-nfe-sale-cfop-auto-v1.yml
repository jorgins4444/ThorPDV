name: One-off NF-e sale CFOP automation v1

on:
  push:
    paths:
      - '.github/scripts/patch-nfe-sale-cfop-auto-v1.py'
      - '.github/workflows/oneoff-nfe-sale-cfop-auto-v1.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply automatic CFOP to sale NF-e flow
        run: python3 .github/scripts/patch-nfe-sale-cfop-auto-v1.py
      - name: Validate project
        run: |
          npm install
          npm run typecheck
          npm run build
          npm run lint
      - name: Commit patch
        run: |
          git config user.name "ThorPDV Automation"
          git config user.email "actions@users.noreply.github.com"
          git add src/app/dashboard/'[...slug]'/nfe-emission-workspace.tsx
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "feat(fiscal): resolve CFOP before NF-e from sale"
          git push
